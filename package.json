{
  "name": "aiappsy-link-engine",
  "version": "1.0.0",
  "description": "Production-ready custom URL shortener & redirect engine for Cloud Run",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "build": "node --check server.js",
    "start": "node server.js",
    "lint": "node --check server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
